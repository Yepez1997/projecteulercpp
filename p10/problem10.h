#include <iostream>
#include <string>
#include <vector>
#include <deque>
#include <iostream> 

using namespace std; 

bool is_prime(int number); 
int sum_primes(std::deque<int> &prime_numbers); 
void check_all_numbers(int limit); 